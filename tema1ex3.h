#include <stdio.h>

// Lembram de decremento? Então, faça um código para exibir os números de 10 a 1 na
// tela do computador, utilizando a estrutura de repetição for

int main() {

    int count;

    for (count = 10; count >= 1; count--)
        printf("%d\n", count);

    return 0;
}




